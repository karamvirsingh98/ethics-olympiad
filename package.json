{
  "name": "ethics-olympiad",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "backend",
    "events",
    "manager",
    "types"
  ],
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "engines": {
    "yarn": "3.2.0"
  },
  "scripts": {
    "dev-all": "clear && cd backend && yarn dev & cd manager && yarn dev & cd events && yarn dev",
    "build-all": "yarn build-backend & yarn build-manager & yarn build-events",
    "deploy-backend": "cd backend && vite build && cd .. git subtree split -P backend/build -b deploy-backend && git push origin deploy-backend",
    "build-manager": "cd manager && yarn build",
    "build-events": "cd events && yarn build"
  }
}
