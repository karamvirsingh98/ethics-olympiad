{
  "name": "ethics-olympiad",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "backend",
    "events",
    "manager",
    "types"
  ],
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "scripts": {
    "dev-all": "clear && cd backend && yarn dev & cd manager && yarn dev & cd events && yarn dev",
    "build-all": "yarn build-backend & yarn build-manager & yarn build-events",
    "build-backend": "git checkout deploy-backend && git merge main --force && git commit -A -m 'deploying backend' && git push origin deploy-backend && git ceckout main",
    "build-manager": "cd manager && yarn build",
    "build-events": "cd events && yarn build",
    "build": "yarn workspace @ethics-olympiad/backend build && echo 'building backend?' ",
    "start": "yarn workspace @ethics-olympiad/backend start"
  }
}
