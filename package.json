{
  "name": "ethics-olympiad",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "backend",
    "events",
    "manager",
    "types"
  ],
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "scripts": {
    "dev-all": "cd backend && yarn dev & cd manager && yarn dev & cd events && yarn dev",
    "build-all": "yarn build-backend & yarn build-manager & yarn build-events",
    "build-backend": "cd backend && yarn build",
    "build-manager": "cd manager && yarn build",
    "build-events": "cd events && yarn build",
    "restart": "cd backend && yarn start"
  }
}

