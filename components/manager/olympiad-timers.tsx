import { Card, CardHeader, CardTitle } from "../ui/card";

export const OlympiadTimers = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Timer Settings</CardTitle>
      </CardHeader>
    </Card>
  );
};
